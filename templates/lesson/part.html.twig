{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        {% include 'lesson/_breadcrumb_lesson.html.twig' %}
        <h2>Introduction</h2>
        <p>
            {{ part.introduction }}
        </p>
        <h2>Méthode</h2>
        <p>
            {{ part.description }}
        </p>
        <h2>Exercice</h2>
        <p>
            {{ part.exercise }}
        </p>
        <h2>Live coding</h2>
        {% for screencast in part.screencasts %}
            {{ screencast.title }}
        {% endfor %}
    </div>
{% endblock %}
